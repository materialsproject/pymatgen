general:
  branches:
    ignore:
      - gh-pages # list of branches to ignore
machine:
  python:
    version: 3.5.2
  environment:
      PATH: $PATH:/home/ubuntu/pymatgen/cmd_line/enum/Linux_64bit:/home/ubuntu/pymatgen/cmd_line/bader/Linux_64bit:/home/ubuntu/pymatgen/cmd_line/gulp/Linux_64bit:/home/ubuntu/pymatgen/cmd_line/aconvasp/Linux_64bit
      GULP_LIB: /home/ubuntu/pymatgen/cmd_line/gulp/Libraries
dependencies:
  override:
    - easy_install -U setuptools
    - pip install -q numpy
    - pip install -r requirements.txt --upgrade
    - pip install -r requirements-optional.txt
    - sudo apt-get install python-openbabel python-vtk python3-tk
    - python setup.py develop
test:
  override:
    - find . -name "*.pyc" -exec rm '{}' \;
    - find . -name "test_*.py" -exec chmod -x '{}' \;
    #- python run_circle.py:
    #    timeout: 720
    - nosetests --config=nose.cfg --cover-html --cover-html-dir=$CIRCLE_ARTIFACTS:
        timeout: 720
    - coveralls
    # Some rudimentary command line tests for pmg
    - pmg structure --convert --filenames test_files/Li2O.cif POSCAR.pmg
    - pmg structure --convert --filenames POSCAR.pmg pmg.cif
    - pmg analyze -d test_files
deployment:
  py27:
    branch: master
    commands:
      - git config --global user.name "Py27 merger"
      - git config --global user.email ongsp@ucsd.edu
      - git config --global push.default matching
      - git checkout stable_py27
      - git pull
      - git merge master -m "Automatic Py27 merge for testing"
      - git push origin stable_py27
