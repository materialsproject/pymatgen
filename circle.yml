dependencies:
  override:
    - pip install -q numpy
    - pip install scipy
    - pip install -r requirements.txt
    - python setup.py install
test:
  override:
    - mv pymatgen pmg
    - ls
    - ls && nosetests:
        environment:
            PATH: $PATH:pmg/cmd_line/enum/Linux_64bit