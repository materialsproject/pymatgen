# setuptools-scm adds every git-tracked file to the sdist; keep it to src/ + metadata.
prune pymatgen-core
prune test-files
prune tests
prune docs
prune dev_scripts
prune .github
exclude uv.lock tasks.py ADMIN.md AGENTS.md CLAUDE.md SECURITY.md .*
