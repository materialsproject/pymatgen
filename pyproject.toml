[build-system]
requires = [
  # pin NumPy version used in the build
  "oldest-supported-numpy",
  "setuptools>=43.0.0",
  "Cython>=0.29.23",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 120
